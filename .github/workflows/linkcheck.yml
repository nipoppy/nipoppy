---
name: Check documentation links

on:
  pull_request:
    branches: ['main']
  schedule:
    - cron: '0 11 * * 0' # Weekly on Sunday at 11:00 UTC
  workflow_dispatch:

env:
  PYTHON_VERSION: "3.11"

jobs:
  sphinx:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7

    - uses: actions/setup-python@v7
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - uses: astral-sh/setup-uv@v10.0.1

    - name: Install package with doc dependencies
      run: |
        uv sync --extra doc

    - name: Generate JSON schemas for docs
      run: |
        uv run python docs/scripts/pydantic_to_jsonschema.py

    - name: Run linkcheck
      run: |
        uv run python -m sphinx -b linkcheck -D linkcheck_timeout=30 docs/source/ docs/build/linkcheck

  pytest:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
    - uses: actions/checkout@v7

    - uses: actions/setup-python@v7
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - uses: astral-sh/setup-uv@v10.0.1

    - name: Install package with test dependencies
      run: |
        uv sync --extra tests

    - name: Run pytest linkcheck tests
      run: |
        uv run python -m pytest -m linkcheck
